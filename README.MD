# For size checker 
# the script checks the size of the file size which ones are 'too large' 
## what the code does 
- stores a list of files as dictionary each with name and size in mb 
- size_mb(box) is a function that loops through the files adds its size and returns the total 
- while loop is used to go through the files and prints whether the size of the is ' too large' or 'fine'
- reads the 'size.txt' back and prints its contents with error handling in case the file doesnt exist 

## how to run 

-press the run button and it will print the function
